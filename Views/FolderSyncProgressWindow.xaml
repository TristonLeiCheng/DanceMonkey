<Window x:Class="DesktopAssistant.Views.FolderSyncProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件夹同步"
        Width="520"
        Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BrushPageBg}">
    <Border Padding="18" Background="{StaticResource BrushSurface}" CornerRadius="14" Margin="16"
            BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       Text="正在同步文件夹"
                       Style="{StaticResource UiSectionTitle}"
                       Margin="0,0,0,6"/>

            <TextBlock x:Name="SummaryText"
                       Grid.Row="1"
                       Style="{StaticResource UiHint}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12"/>

            <Grid Grid.Row="2" Margin="0,0,0,8">
                <ProgressBar x:Name="SyncProgressBar" Height="10" Minimum="0"/>
                <TextBlock x:Name="PercentText"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="11"
                           Foreground="{StaticResource BrushTextSecondary}"/>
            </Grid>

            <Border Grid.Row="3"
                    Background="{StaticResource BrushSurfaceMuted}"
                    BorderBrush="{StaticResource BrushBorderSubtle}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="10">
                <TextBlock x:Name="CurrentText"
                           Text="准备同步..."
                           TextWrapping="Wrap"
                           Foreground="{StaticResource BrushTextPrimary}"/>
            </Border>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,14,0,0">
                <Button x:Name="CancelButton"
                        Content="取消"
                        MinWidth="88"
                        Style="{StaticResource UiBtnSecondary}"
                        Click="Cancel_OnClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
